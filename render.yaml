services:
  - type: web
    name: israel-campaigns-api
    runtime: node
    rootDir: api
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: ANTHROPIC_API_KEY
        sync: false
